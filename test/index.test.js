/* --------------------
 * zip-random-access module
 * Tests
 * ------------------*/

'use strict';

// Modules
const zip = require('zip-random-access');

// Init
require('./support/index.js');

// Tests

describe('tests', () => {
	it.skip('all', () => { // eslint-disable-line jest/no-disabled-tests
		expect(zip).not.toBeUndefined();
	});
});
